# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TimeLogApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TimeLogApp
  pod "Alamofire"
  pod 'SwiftyJSON'
  pod 'Eureka'

  target 'TimeLogAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TimeLogAppUITests' do
    # Pods for testing
  end

end
